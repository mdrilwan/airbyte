Run below commands to setup the project

```
    poetry init
    poetry env activate
```

Activate the virtual environment

Install dependencies and start the application

```
    poetry install
    poetry run python pyairbyte_impl.py
```